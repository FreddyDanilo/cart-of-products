export type Product = {
  id: string;
  name: string;
  price: number;
  imageURL: string;
};

export type ProductCart = {
  id: string;
  name: string;
  imageURL: string;
  price: number;
  quantity: number;
};
